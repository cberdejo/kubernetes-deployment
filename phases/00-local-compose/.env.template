POSTGRES_USER=user
POSTGRES_PASSWORD=password
POSTGRES_DB=data_agency_db

# Host y puerto del backend (Compose fuerza el mismo valor dentro del contenedor y en el mapeo de puertos)
BACKEND_HOST=0.0.0.0
BACKEND_PORT=9090
DATABASE_URI=postgres://user:password@postgres:5432/data_agency_db

# Optional variables for Kubernetes / other deployments
FRONTEND_HOST=localhost
